# Dependencias de producción — API web + auth + BD (Fase 8).
#
# Para el pipeline de datos y ML (entrenar modelos, /predict, /train) instala
# además requirements-ml.txt. Para ejecutar los tests, requirements-dev.txt.
#
#   pip install -r requirements.txt -r requirements-ml.txt   # API completa
#   pip install -r requirements-dev.txt                      # + tests

fastapi==0.136.1
uvicorn[standard]==0.46.0
sqlmodel==0.0.37
SQLAlchemy==2.0.48
pydantic==2.12.5
pydantic-settings==2.14.0
python-jose[cryptography]==3.5.0
argon2-cffi==25.1.0
psycopg2-binary==2.9.11
email-validator==2.3.0
