# URL raíz del backend FastAPI que consume el frontend.
# En desarrollo local puede omitirse: el código cae a http://localhost:8000 por defecto.
# Definir solo para apuntar a otro backend (p. ej. al desplegar).
VITE_API_URL=http://localhost:8000
